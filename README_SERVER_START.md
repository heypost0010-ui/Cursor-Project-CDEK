# Инструкция по запуску сервера

## Проблема: Порт 3000 уже занят

Если при запуске сервера вы видите ошибку:
```
Error: listen EADDRINUSE: address already in use :::3000
```

Это означает, что порт 3000 уже занят другим процессом.

## Решения

### Вариант 1: Использовать скрипт автоматической проверки

```powershell
npm run start:check
```

Скрипт автоматически:
- Проверит, занят ли порт 3000
- Предложит остановить процесс, если порт занят
- Запустит сервер

### Вариант 2: Остановить процесс вручную

1. **Найти процесс, занимающий порт:**
```powershell
netstat -ano | findstr :3000
```

2. **Остановить процесс по PID:**
```powershell
taskkill /PID <PID> /F
```

Например, если PID = 15556:
```powershell
taskkill /PID 15556 /F
```

3. **Запустить сервер:**
```powershell
npm start
```

### Вариант 3: Использовать другой порт

```powershell
$env:PORT=3001; npm start
```

Или создать файл `.env` с:
```
PORT=3001
```

## Проверка работы сервера

После запуска сервер должен вывести:
```
🚀 Сервер запущен!
📍 URL: http://localhost:3000
```

Проверить работу можно:
```powershell
Invoke-WebRequest -Uri http://localhost:3000/ -UseBasicParsing
```

## Улучшения в коде

В `src/server.js` добавлена обработка ошибок:
- Понятное сообщение об ошибке, если порт занят
- Инструкции по решению проблемы
- Корректное завершение процесса при ошибке

