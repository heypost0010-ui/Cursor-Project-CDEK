# CDEK Delivery API

Backend API для расчета стоимости доставки CDEK на странице товара.

## Возможности

- 🔍 **Поиск городов** - Поиск городов по названию с поддержкой частичного совпадения
- 💰 **Расчет стоимости** - Расчет по всем доступным тарифам или по конкретному тарифу
- 📦 **Умный расчет** - Автоматический поиск ПВЗ для тарифов склад-склад
- 🏢 **Работа с ПВЗ** - Получение списка офисов с фильтрацией по типу и поддержке сборного груза
- 📋 **Список тарифов** - Получение всех доступных тарифов CDEK
- 📝 **Создание заказов** - Создание заказов на доставку с поддержкой доставки до ПВЗ и до двери
- 🔐 **Авторизация** - Автоматическое управление OAuth токенами с кэшированием
- ✅ **Валидация** - Полная валидация входных данных на всех эндпоинтах
- 🛡️ **Безопасность** - Обработка ошибок без раскрытия внутренней структуры

**Подробное описание всех возможностей:** [docs/API_FUNCTIONALITY.md](docs/API_FUNCTIONALITY.md)

## Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

Файл `.env` уже содержит тестовые учетные данные CDEK для разработки.

### 3. Запуск сервера

```bash
npm start
```

Или в режиме разработки с автоперезагрузкой:

```bash
npm run dev
```

Сервер запустится на `http://localhost:3000`

## API Эндпоинты

### 1. Поиск городов

**GET** `/api/delivery/cities`

**Параметры:**
- `q` (обязательный) - Название города
- `country_code` (опционально) - Код страны (по умолчанию `RU`)
- `size` (опционально) - Ограничение выборки (по умолчанию `10`)

**Пример запроса:**
```bash
curl "http://localhost:3000/api/delivery/cities?q=Москва"
```

**Пример ответа:**
```json
{
  "success": true,
  "data": [
    {
      "code": 44,
      "city": "Москва",
      "region": "Московская область",
      "region_code": 77,
      "country": "Россия",
      "country_code": "RU"
    }
  ],
  "count": 1
}
```

### 2. Расчет стоимости доставки

**POST** `/api/delivery/calculate`

**Тело запроса:**
```json
{
  "fromCityCode": 44,
  "fromAddress": "г. Москва, ул. Примерная, д. 1",
  "toCityCode": 270,
  "toAddress": "г. Новосибирск",
  "weight": 2000,
  "length": 10,
  "width": 20,
  "height": 30
}
```

**Параметры:**
- `fromCityCode` (обязательный) - Код города отправления
- `fromAddress` (обязательный) - Адрес отправления
- `toCityCode` (обязательный) - Код города доставки
- `toAddress` (обязательный) - Адрес доставки
- `weight` (обязательный) - Вес в граммах
- `length` (обязательный) - Длина в см
- `width` (обязательный) - Ширина в см
- `height` (обязательный) - Высота в см

**Пример запроса:**
```bash
curl -X POST http://localhost:3000/api/delivery/calculate \
  -H "Content-Type: application/json" \
  -d '{
    "fromCityCode": 44,
    "fromAddress": "г. Москва, ул. Примерная, д. 1",
    "toCityCode": 270,
    "toAddress": "г. Новосибирск",
    "weight": 2000,
    "length": 10,
    "width": 20,
    "height": 30
  }'
```

**Пример ответа:**
```json
{
  "success": true,
  "tariffs": [
    {
      "code": 136,
      "name": "Посылка склад-склад",
      "description": "Доставка до склада СДЭК",
      "cost": 350.00,
      "periodMin": 1,
      "periodMax": 2,
      "calendarMin": 1,
      "calendarMax": 2
    },
    {
      "code": 137,
      "name": "Посылка склад-дверь",
      "description": "Доставка до двери",
      "cost": 450.00,
      "periodMin": 1,
      "periodMax": 2,
      "calendarMin": 1,
      "calendarMax": 2
    }
  ],
  "deliveryDateRange": {
    "dateMin": "2025-03-25",
    "dateMax": "2025-03-26"
  },
  "errors": [],
  "warnings": []
}
```

### 3. Расчет по конкретному тарифу

**POST** `/api/delivery/calculate-by-tariff`

**Тело запроса:**
```json
{
  "tariffCode": 136,
  "fromCityCode": 44,
  "fromAddress": "г. Москва, ул. Примерная, д. 1",
  "toCityCode": 270,
  "toAddress": "г. Новосибирск",
  "weight": 2000,
  "length": 10,
  "width": 20,
  "height": 30,
  "services": [
    {
      "code": "INSURANCE",
      "parameter": "5000"
    }
  ]
}
```

### 4. Получение списка офисов (ПВЗ)

**GET** `/api/delivery/offices?cityCode=44`

**Параметры:**
- `cityCode` (обязательный) - Код города
- `type` (опционально) - Тип офиса: `PVZ`, `POSTAMAT`, `ALL` (по умолчанию `ALL`)

### 5. Получение списка доступных тарифов

**GET** `/api/delivery/tariffs?lang=rus`

**Параметры:**
- `lang` (опционально) - Язык ответа: `rus`, `eng`, `zho` (по умолчанию `rus`)

**Пример ответа:**
```json
{
  "success": true,
  "data": [
    {
      "tariff_code": 136,
      "tariff_name": "Посылка склад-склад",
      "tariff_description": "Доставка до склада СДЭК",
      "delivery_mode": 4
    }
  ],
  "count": 1
}
```

## Документация

- **[API_FUNCTIONALITY.md](docs/API_FUNCTIONALITY.md)** - Полное описание функционала бэкенда и всех доступных эндпоинтов
- **[TARIFFS_REFERENCE.md](docs/TARIFFS_REFERENCE.md)** - Справочник по тарифам для сборного груза
- **[README_CUSTOM_WIDGET.md](README_CUSTOM_WIDGET.md)** - Документация по виджету расчета доставки

## Виджет расчета доставки

В проекте включен кастомный виджет для расчета стоимости доставки. Подробная документация доступна в [README_CUSTOM_WIDGET.md](README_CUSTOM_WIDGET.md).

### Быстрый старт с виджетом:

1. Запустите сервер:
```bash
npm start
```

2. Откройте `custom-delivery-widget.html` в браузере

3. Выберите город доставки и тип доставки (до ПВЗ или до двери)

## Тестирование

### Пример полного флоу:

1. **Поиск города отправления:**
```bash
curl "http://localhost:3000/api/delivery/cities?q=Москва"
```

2. **Поиск города доставки:**
```bash
curl "http://localhost:3000/api/delivery/cities?q=Новосибирск"
```

3. **Расчет стоимости доставки:**
```bash
curl -X POST http://localhost:3000/api/delivery/calculate \
  -H "Content-Type: application/json" \
  -d '{
    "fromCityCode": 44,
    "fromAddress": "г. Москва",
    "toCityCode": 270,
    "toAddress": "г. Новосибирск",
    "weight": 2000,
    "length": 10,
    "width": 20,
    "height": 30
  }'
```

## Структура проекта

```
.
├── src/
│   ├── cdek/
│   │   ├── auth.js      # Модуль авторизации
│   │   └── api.js       # Клиент CDEK API
│   ├── routes/
│   │   └── delivery.js  # Роуты API
│   └── server.js        # Основной сервер
├── docs/
│   ├── TARIFFS_REFERENCE.md  # Справочник по тарифам
│   └── API_FUNCTIONALITY.md  # Описание функционала API
├── archive/             # Архив тестовых файлов и промежуточных документов
├── custom-delivery-widget.html  # Виджет расчета доставки
├── .env.example         # Пример конфигурации
├── package.json
├── README.md
└── README_CUSTOM_WIDGET.md  # Документация по виджету
```

## Конфигурация

### Переменные окружения

- `CDEK_API_URL` - URL API CDEK (тестовый: `https://api.edu.cdek.ru/v2`)
- `CDEK_ACCOUNT` - Account для авторизации
- `CDEK_SECURE_PASSWORD` - Secure password для авторизации
- `PORT` - Порт сервера (по умолчанию `3000`)
- `NODE_ENV` - Режим работы (`development` / `production`)

### Тестовые учетные данные

Для разработки используются тестовые учетные данные:
- **Account**: `wqGwiQx0gg8mLtiEKsUinjVSICCjtTEP`
- **Secure password**: `RmAmgvSgSl1yirlz9QupbzOJVqhCxcP5`
- **API URL**: `https://api.edu.cdek.ru/v2`

⚠️ **Важно**: Тестовые заказы не отображаются в личном кабинете и не обрабатываются.

## Переход на продакшн

Для продакшн используйте:
1. Получите учетные данные в личном кабинете CDEK (раздел "Интеграция")
2. Измените в `.env`:
   ```
   CDEK_API_URL=https://api.cdek.ru/v2
   CDEK_ACCOUNT=ваш_аккаунт
   CDEK_SECURE_PASSWORD=ваш_пароль
   ```

## Единицы измерения

- **Вес**: граммы
- **Размеры**: сантиметры (длина, ширина, высота)

## Обработка ошибок

API возвращает ошибки в формате:
```json
{
  "error": "Описание ошибки",
  "message": "Детали ошибки",
  "details": ["Список ошибок валидации"]
}
```

## Лицензия

MIT


